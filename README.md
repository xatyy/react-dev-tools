1. Run git clone `https://github.com/xatyy/react-dev-tools`
2. Go into react-dev-tools folder
3. Access in your Chrome browser `chrome://extension` and turn on Developer mode (top-right corner)
4. Drag'n'drop the `.crx` file into the Chrome Extension Page and install.
